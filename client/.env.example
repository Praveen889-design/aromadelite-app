# Origin of the deployed Aromadelite API. Leave blank for local dev (the CRA
# proxy in package.json forwards requests to http://localhost:5050).
#
# Example for production (Vercel):
#   REACT_APP_API_URL=https://aromadelite-api.up.railway.app
REACT_APP_API_URL=
